{
  "name": "enum-converter",
  "private": true,
  "author": "Nitzan Ohana",
  "scripts": {
    "build": "yarn workspaces run build",
    "clean": "yarn workspaces run clean",
    "test": "yarn workspaces run test",
    "copy-static": "mv ./packages/enum-converter-web/build ./packages/enum-converter-api/static",
    "heroku-postbuild": "yarn run build && yarn run copy-static",
    "start": "yarn workspace enum-server-api run start"
  },
  "engines": {
    "yarn": "^1.10.0"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/jest",
      "**/jest/**"
    ]
  }
}
