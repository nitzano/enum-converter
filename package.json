{
  "name": "enum-converter",
  "private": true,
  "author": "Nitzan Ohana",
  "scripts": {
    "build": "yarn workspaces run build",
    "clean": "yarn workspaces run clean",
    "dev": "yarn workspaces run start",
    "heroku-postbuild": "yarn run build",
    "start": "yarn workspace enum-server-api run start"
  },
  "engines": {
    "yarn": "^1.10.0"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/jest",
      "**/jest/**"
    ]
  }
}
